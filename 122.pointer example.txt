#include <bits/stdc++.h> 
using namespace std; 
void vimal() 
{ 
	int var = 20; 
	int* ptr; 
	ptr = &var; 
	cout << "Value at ptr = " << ptr << "\n"; 
	cout << "Value at var = " << var << "\n"; 
	cout << "Value at *ptr = " << *ptr << "\n"; 
} 
int main() 
{ 
vimal(); 
return 0; 
}
